import std::types;
import std::builtin;
import std::debug;
import std::console;

async i32 compute() {
    return 42;
}

async i32 main() {
    printf("%d\n", await compute());
    void* value = await Console.readLine();
    printf("%s\n", value);
    return 1;
}