import std::types;
import std::collections;

namespace playground;

i32 main() {
    array<i32> arr = array<i32>();
    arr.push(10);
    arr.push(20);
    printf("arr = %d", arr.get(0));
    return 0;
}