import std::types;

namespace playground;

struct user {
    i32 id;
    i32 age;

    user(i32 id, i32 age) {
        this.id = id;
        this.age = age;
    }
}

void main() {
    user me = new user(1, 69);
    printf("age %d\n", (1 + 2 * 3 / 2 + 5 * 9));
}
