import std::types;
import std::collections;

namespace playground;

struct Box<T> {
    T value;

    Box(T v) {
        this.value = v;
    }
}

i32 main() {
    Box<i32> b = new Box<i32>(99);
    return b.value;
}