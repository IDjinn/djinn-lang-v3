import std::types;
import std::collections;
import std::libc;


// map<i32, i8*> m = map<i32, i8*>();
// m.set(420, "Hello");
// m.set(69, "World");

// printf("hello %s\n", m.get(69));

i32 main() {
    i64 first = 69;
    i32 test = (i32)first;
    return test;
}
