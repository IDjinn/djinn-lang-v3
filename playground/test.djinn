import std::types;
import std::collections;
import std::libc;

// i32 main() {
//     i64 first = 69;
//     map<i32, i8*> m = map<i32, i8*>();
//     m.set(420, "Hello");
//     m.set(69, "World");
//
//     printf("hello %s\n", m.get(69));
//     return 240;
// }

extern "C" {
  i32 printf(i8* fmt, ...);
}

async i32 compute(i32 x) {
  return x * 2 + 1;
}

async i32 main() {
  i32 result = await compute(10);
  printf("result: %d\n", result);  // result: 21
  return 0;
}

