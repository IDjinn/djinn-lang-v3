import std::types;

extern "C" {
  c_result puts(i8* s);
  c_result printf(i8* fmt, ...);
  void* malloc(size_long size);
  void free(void* ptr);
  c_result strlen(i8* s);
  c_result write(size file_descriptor, void* buf, size count);
}
